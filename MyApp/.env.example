# Use host IP (not localhost) so physical device / emulator can reach your machine
# AllSpaces API
EXPO_PUBLIC_API_BASE_URL=http://198.18.1.206:8080/api
EXPO_PUBLIC_API_KEY=

# Intent Layer: /api/intent/parse, /api/intent/search-by-prompt
EXPO_PUBLIC_INTENT_API_BASE_URL=http://198.18.1.206:3001/api

# Optional: for authenticated search, store session-id via app login
# The app uses expo-secure-store for SESSION_ID when available
