import { appSpacings, moderateScale } from "@/theme";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  ratingStar: {
    marginRight: moderateScale(appSpacings[1]),
  },
});
